(function( $ ) {
	'use strict';




      $(document).ready(function() {
		// wait until we are really sure VC is loaded
		$(window).ready(function() {


			// check for a blank vcmegafooter custom post
			// on a blank vcmegafooter custom post, we should enable the VC editor by clciking the button

			// var mgf_vc_button = '.composer-switch a.wpb_switch-to-composer';
			// var mgf_vc_existingcontent = 'textarea#content';
			// if( $(mgf_vc_button).length && !$(mgf_vc_existingcontent).val() ) {
			// 	$(mgf_vc_button).click()
			// }


			//
			// disable and the front end editor for now, indiciate via grey colour
			// just backend editor for footer blocks, lets keep this part simple
			//
			//
			$('a.wpb_switch-to-front-composer').css('pointer-events','none');
			$('a.wpb_switch-to-front-composer').css('background-color','#444444');
			$('.vc_pull-right a#wpb-edit-inline').css('pointer-events','none');
			$('.vc_pull-right a#wpb-edit-inline').css('background-color','#444444');




		});
	});


      // for any metaboxes we create with multiselect, we shall use
      // https://github.com/nobleclem/jQuery-MultiSelect
      $('.vc-mega-footer-field').show();

      $('#vcmegafooter-showonpages').multiselect({ texts: { placeholder: 'All pages' } });
      $('#vcmegafooter-showonposts').multiselect({ texts: { placeholder: 'All posts' } });
      $('#vcmegafooter-showonpostcategories').multiselect({ texts: { placeholder: 'All categories' } });
      $('#vcmegafooter-showonposttags').multiselect({ texts: { placeholder: 'All tags' } });



})( jQuery );
